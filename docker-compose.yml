services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: stalker
      POSTGRES_PASSWORD: stalker
      POSTGRES_DB: stalker_os
    volumes:
      - stalker_pg:/var/lib/postgresql/data

volumes:
  stalker_pg:
